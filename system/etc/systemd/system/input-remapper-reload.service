[Unit]
Description=Reload input-remapper presets on device reconnect
After=input-remapper-daemon.service

[Service]
Type=oneshot
ExecStartPre=/bin/sleep 3
ExecStart=/usr/bin/input-remapper-control --command stop-all
ExecStart=/usr/bin/input-remapper-control --command autoload
